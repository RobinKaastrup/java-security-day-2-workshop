server:
  port: 4000
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false

spring:
  datasource:
    url: jdbc:postgresql://abul.db.elephantsql.com:5432/jksmptkm
    username: vqxaokzu
    password: QDGvUn5oMJjTEIvBGZXw3O0Cr2c2BjYO
    hikari:
      maximum-pool-size: 3
      minimum-idle: 3
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  booleanuk:
    app:
      jwtSecret: ====================================BoleanUK====================================
      jwtExpirationMs: 86400000